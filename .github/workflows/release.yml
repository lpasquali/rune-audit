# SPDX-License-Identifier: Apache-2.0
# Container image release (GHCR) + optional SBOM / SLSA via shared rune-ci workflow.
# Triggers only on version tags (v*). PyPI publishing is separate: publish-pypi.yml.
name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write
  packages: write
  id-token: write
  attestations: write

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  release:
    uses: lpasquali/rune-ci/.github/workflows/release.yml@72ffc70b5253c30afd47b06e81eff3f9b08c04c8
    with:
      image-name: "rune-audit"
      generate-sbom: true
      slsa-attestation: true
    secrets: inherit
