# SPDX-License-Identifier: Apache-2.0
# Container image release (GHCR) + optional SBOM / SLSA via shared rune-ci workflow.
# Triggers only on version tags (v*). PyPI publishing is separate: publish-pypi.yml.
name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write
  packages: write
  id-token: write
  attestations: write

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  release:
    uses: lpasquali/rune-ci/.github/workflows/release.yml@cfdb81c2d4f03bb82786729401fe7b8e833e4347
    with:
      image-name: "rune-audit"
      generate-sbom: true
      slsa-attestation: true
    secrets: inherit
